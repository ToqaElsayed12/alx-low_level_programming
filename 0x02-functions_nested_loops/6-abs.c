#include "main.h"

/**
 * _abs - function that computes the absolute
 *	value of an integer
 *
 * @n: Always 0 (success)
*/
int main(void)
{
    int r;

	if (n < 0)
	{
		n = (-1) * n;
	}
	return (n);

}
